//#pragma once
//#include <iostream>
//#include <string>
//#include <vector>
//#include <algorithm>
//#include <iterator>

//using namespace std;

//struct Include {
//	void clear();
//	std::string headerfile;
//	bool is_quotation;
//	bool is_angle;

//	friend std::ostream& operator<<(std::ostream &s, const Include &I);
//};

//std::ostream& operator<<(std::ostream &s, const Include &I);

////下面的 这个宏用来取消Lex和Yacc默认的YYSTYPE定义，因为默认的
//// YYSTYPE定义仅仅只能够记录整数信息，因此要保存额外的信息必须这样定义宏，可以参见Yacc

////自动生成的标记头文件 frame.tab.h

//#define YYSTYPE Include

